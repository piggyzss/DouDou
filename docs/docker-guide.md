# Docker 知识指南

## 📋 概述

本指南通过生动的比喻和实例，帮助你理解 Docker 的核心概念和工作原理。无论你是 Docker 新手还是想深入理解容器技术，这份指南都能为你提供清晰的知识体系。

---

## 🏠 核心概念：房屋建造比喻

为了更好地理解 Docker，我们用**建造房屋**这个统一的比喻来解释所有概念。

---

### 1. Dockerfile = 房屋设计图纸

**定义**: 一个文本文件，包含构建 Docker 镜像的所有指令。

**房屋比喻**: 建筑师绘制的详细施工图纸，记录了如何一步步建造房屋。

**示例**:
```dockerfile
# 选择地基类型
# 选择基础镜像（带 Python 3.11 的轻量级 Linux 系统）
FROM python:3.11-slim

# 规划房间布局
# 在容器内创建并进入 /app 目录
# 这是容器的文件系统，不是你的主机（电脑）
WORKDIR /app

# 列出需要的建材清单
# 把主机的 requirements.txt 复制到容器的 /app/requirements.txt
COPY requirements.txt .

# 采购并安装建材
# 在容器的 /app 目录下安装 Python 依赖
RUN pip install -r requirements.txt

# 搬入家具和装饰
# 第一个 .（源路径）: 你的主机上的当前目录（Dockerfile 所在目录）
# 第二个 .（目标路径）: 容器内的当前工作目录（/app）
COPY . .

# 标注房屋用途
# exec 形式
# - 直接执行程序，不通过 shell
# - 信号处理更好（Ctrl+C 可以正常停止）
# - 性能更好
# 等价于在容器内执行：uvicorn app.main:app --host 0.0.0.0 --port 8000

# uvicorn:
# ASGI 服务器，用于运行 FastAPI/Starlette 应用
# 必须已经通过 pip install 安装

# app.main:app:
# app.main: Python 模块路径（app/main.py 文件）
# :app: 该文件中的 FastAPI 应用实例变量名
# 相当于 from app.main import app

# --host 0.0.0.0:
# 监听所有网络接口
# 允许外部访问（如果只用 127.0.0.1 则只能容器内部访问）
# 这样主机才能通过端口映射访问容器

# --port 8000:
# 在容器内监听 8000 端口
# 需要配合 docker run -p 8000:8000 才能从主机访问
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

**发生了什么**：
- Docker 读取 Dockerfile，在容器内创建 /app 目录
- 把你电脑上的代码复制到容器的 /app/
- 在容器内安装依赖
- 启动 uvicorn 服务，监听容器的 8000 端口
- 通过 -p 8000:8000 把容器的 8000 端口映射到主机的 8000 端口
- 你在浏览器访问 localhost:8000 就能访问到容器内的服务

**特点**:
- 📝 纯文本文件，可以版本控制
- 🔄 可以复制无数份，建造多栋相同房屋
- 📋 每一行都是一个施工步骤
- 👷 任何人拿到图纸都能建出一样的房子

**关键指令**:
- `FROM`: 选择基础镜像（地基类型）
- `WORKDIR`: 设置工作目录（房间布局）
- `COPY`: 复制文件（搬运物品）
- `RUN`: 执行命令（施工操作）
- `CMD`: 容器启动命令（房屋用途）
- `EXPOSE`: 声明端口（门牌号）
- `ENV`: 设置环境变量（房屋配置）

---

### 2. 镜像 (Image) = 预制房屋模块

**定义**: 根据 Dockerfile 构建出的只读模板，包含运行应用所需的所有内容。

**房屋比喻**: 在工厂按图纸预制好的标准化房屋模块，像乐高积木一样可以快速组装。

**构建镜像**:
```bash
# 基本构建
docker build -t my-app .

# 指定 Dockerfile
docker build -f Dockerfile.dev -t my-app-dev .

# 不使用缓存
docker build --no-cache -t my-app .
```

**特点**:
- 📦 在工厂预制完成，质量统一
- 🏭 可以批量生产
- 🚚 可以运输到任何地方（Docker Hub）
- 🔒 只读的，不能直接修改
- ♾️ 一套模块可以复制无数套

**镜像分层**:
```
镜像 = 多层叠加
├── Layer 1: 基础系统（地基）
├── Layer 2: 运行时环境（框架）
├── Layer 3: 依赖包（设备）
└── Layer 4: 应用代码（装修）
```

**常用命令**:
```bash
# 查看镜像列表
docker images

# 删除镜像
docker rmi 镜像名称

# 下载镜像
docker pull 镜像名称

# 查看镜像详情
docker inspect 镜像名称

# 查看镜像构建历史
docker history 镜像名称
```

---

### 3. 容器 (Container) = 实际建成的房屋

**定义**: 镜像的运行实例，是应用实际运行的环境。

**房屋比喻**: 用预制模块在工地上实际组装建成的房屋，可以入住使用。

**创建和运行容器**:
```bash
# 基本运行
docker run my-app

# 后台运行
docker run -d my-app

# 指定名称和端口
docker run -d --name my-container -p 8000:8000 my-app

# 挂载卷
docker run -d -v ./data:/app/data my-app

# 设置环境变量
docker run -d -e DEBUG=true my-app
```

**特点**:
- 🏡 真实运行的环境，可以使用
- 🚪 有独立的 ID 和名称（门牌号）
- 🔧 可以修改内部内容（但重启后恢复）
- 🔄 可以启动、停止、重启
- 🗑️ 可以删除

**镜像与容器的关系**:
```
📦 镜像（模板）          🏠 容器（实例）
   my-app-image    →    container-1
                   →    container-2
                   →    container-3

一个镜像可以创建多个容器
一个容器只能从一个镜像创建
```

**类比**:
- **镜像** = 游戏安装包（只有一个）
- **容器** = 游戏运行窗口（可以开多个）

**常用命令**:
```bash
# 查看运行中的容器
docker ps

# 查看所有容器
docker ps -a

# 启动/停止/重启容器
docker start 容器名称
docker stop 容器名称
docker restart 容器名称

# 删除容器
docker rm 容器名称

# 查看日志
docker logs 容器名称
docker logs -f 容器名称  # 实时查看

# 进入容器
docker exec -it 容器名称 /bin/bash

# 查看容器详情
docker inspect 容器名称

# 查看资源使用
docker stats 容器名称
```

---

### 4. Docker Compose = 小区总体规划

**定义**: 用于定义和运行多容器 Docker 应用的工具。

**房屋比喻**: 整个住宅小区的总体规划图，管理多栋建筑的协同工作。

**配置文件示例**:
```yaml
# docker-compose.yml
version: '3.8'

services:
  # 主办公楼
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DEBUG=true
    networks:
      - app-network
    depends_on:
      - redis
  
  # 仓储配套楼
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - app-network

# 基础设施
volumes:
  redis_data:

networks:
  app-network:
    driver: bridge
```

**特点**:
- 🏘️ 管理多个容器（多栋建筑）
- 📋 一个配置文件定义所有服务
- 🔄 一键启动/停止所有服务
- 🛣️ 自动创建网络连接
- 📦 自动管理卷和依赖

**常用命令**:
```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 查看服务状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 重启服务
docker-compose restart

# 重新构建并启动
docker-compose up -d --build

# 执行命令
docker-compose exec 服务名 命令
```

**说明**：
使用Docker命令还是Docker Compose？
简单判断标准
✅ 多服务协同 - 自动处理启动顺序、网络连接
✅ 版本控制 - YAML 文件提交到 Git，团队配置一致
✅ 配置管理 - 声明式配置，清晰易维护
vs.
❌ 一堆冗长的命令脚本，难以维护和协作

单个容器，简单测试
  → docker run

多个容器，需要协同工作
  → Docker Compose

生产环境，大规模部署
  → Kubernetes (更高级的编排工具)

---

### 5. 卷 (Volume) = 保险箱室

**定义**: 用于持久化存储数据的机制，独立于容器生命周期。

**房屋比喻**: 小区的独立保险箱室，即使房子拆了，保险箱里的贵重物品还在。

**卷的类型**:

**1. 命名卷 (Named Volume)**
```yaml
volumes:
  # Redis 数据持久化
  - redis_data:/data  # Docker 管理的独立存储
```
**比喻**: 小区统一管理的保险箱室

**2. 绑定挂载 (Bind Mount)**
```yaml
volumes:
  # 绑定挂载：代码热重载（开发环境）
  - ./agent-backend:/app  # 主机代码目录 → 容器工作目录
  
  # 绑定挂载：日志持久化
  - ./logs:/app/logs  # 直接映射到主机目录
```
**比喻**: 直连你家仓库的通道

**使用场景**:
- **开发环境代码热重载**: 在主机上修改代码，容器内立即生效，无需重新构建镜像
- **日志持久化**: 容器删除后，日志文件仍保留在主机
- **配置文件共享**: 主机和容器共享配置文件，方便调试

**3. 临时卷 (tmpfs)**
```yaml
tmpfs:
  - /tmp  # 内存中的临时存储
```
**比喻**: 临时储物柜，关门就清空

**特点**:
- 💾 数据持久化，容器删除后数据不丢失
- 🔄 可以在多个容器间共享
- 📂 独立于容器生命周期
- 🚀 命名卷性能优于绑定挂载

**实际应用示例**:

```yaml
# docker-compose.yml 示例
services:
  backend:
    volumes:
      # 开发环境：代码热重载
      - ./agent-backend:/app:cached
      # 你在 VSCode 修改 main.py，容器内立即生效
      
      # 日志持久化
      - ./logs:/app/logs
      # 容器重启，日志不丢失
  
  redis:
    volumes:
      # 数据库数据持久化
      - redis_data:/data
      # Redis 数据不会因容器删除而丢失

volumes:
  redis_data:  # 声明命名卷
```

**绑定挂载的工作原理**:
```
主机（你的电脑）              Docker 容器
/Users/you/project/            /app/
├── agent-backend/       ←→   (实时同步)
│   ├── main.py               ├── main.py
│   └── config.py             └── config.py
└── logs/                ←→   /app/logs/
    └── app.log               └── app.log

主机修改文件 → 容器内立即看到
容器内修改文件 → 主机上立即看到
```

**常用命令**:
```bash
# 创建卷
docker volume create my-volume

# 查看卷列表
docker volume ls

# 查看卷详情
docker volume inspect my-volume

# 删除卷
docker volume rm my-volume

# 清理未使用的卷
docker volume prune
```

---

### 6. 网络 (Network) = 小区道路系统

**定义**: 容器之间通信的网络层。

**房屋比喻**: 小区内部的道路系统，让建筑之间可以互相访问。

**网络类型**:

**1. Bridge（桥接网络）**
```bash
docker network create my-network
```
**比喻**: 小区内部道路，容器间可以互相访问
**用途**: 默认网络类型，适合单主机多容器

**2. Host（主机网络）**
```bash
docker run --network host my-app
```
**比喻**: 直接使用外部街道，没有隔离
**用途**: 容器直接使用主机网络，性能最好

**3. None（无网络）**
```bash
docker run --network none my-app
```
**比喻**: 完全封闭的建筑，没有道路连接
**用途**: 完全隔离的容器

**网络通信**:
```
小区内部通信（容器间）:
  backend ──道路──→ redis
  可以用服务名访问: redis:6379

小区外部访问（主机到容器）:
  localhost:8000 ──大门──→ backend
  需要通过端口映射
```

**常用命令**:
```bash
# 创建网络
docker network create my-network

# 查看网络列表
docker network ls

# 查看网络详情
docker network inspect my-network

# 连接容器到网络
docker network connect my-network 容器名称

# 断开连接
docker network disconnect my-network 容器名称

# 删除网络
docker network rm my-network
```

---

### 7. 端口映射 = 门牌号系统

**定义**: 将容器内部端口映射到主机端口，使外部可以访问。

**房屋比喻**: 小区内部有自己的门牌号，需要映射到外部街道才能让访客找到。

**端口映射语法**:
```bash
# -p 主机端口:容器端口
docker run -p 8000:8000 my-app

# 映射到随机端口
docker run -P my-app

# 指定 IP
docker run -p 127.0.0.1:8000:8000 my-app

# 多个端口
docker run -p 8000:8000 -p 6379:6379 my-app
```

**映射示例**:
```
外部街道（主机）         小区内部（容器）
localhost:8000    →     容器:8000
localhost:3000    →     容器:3000
localhost:6379    →     容器:6379
```

**查看端口映射**:
```bash
# 查看容器端口
docker port 容器名称

# 输出示例:
# 8000/tcp -> 0.0.0.0:8000
```

---

## 🔄 Docker 工作流程

### 完整流程图

```
1. 编写 Dockerfile（绘制图纸）
   ↓
2. docker build（工厂生产预制模块）
   ↓
3. 镜像 Image（预制模块入库）
   ↓
4. docker run（用模块组装房屋）
   ↓
5. 容器 Container（房屋开始运营）
   ↓
6. docker stop（关闭房屋）
   ↓
7. docker rm（拆除房屋）
```

### 实际开发流程

```bash
# 1. 编写 Dockerfile
vim Dockerfile

# 2. 构建镜像
docker build -t my-app:v1.0 .

# 3. 运行容器
docker run -d --name my-container -p 8000:8000 my-app:v1.0

# 4. 查看日志
docker logs -f my-container

# 5. 进入容器调试
docker exec -it my-container /bin/bash

# 6. 停止容器
docker stop my-container

# 7. 删除容器
docker rm my-container

# 8. 删除镜像
docker rmi my-app:v1.0
```

---

## 🎯 Docker vs 传统部署

### 传统方式的问题

```
开发环境:
  Python 3.9 + 依赖A版本1.0
  ↓
测试环境:
  Python 3.10 + 依赖A版本1.5
  ↓
生产环境:
  Python 3.11 + 依赖A版本2.0

结果: 😱 "在我电脑上能跑啊！"
```

### Docker 的解决方案

```
所有环境:
  Docker 容器（Python 3.11 + 固定依赖）
  ↓
  完全相同的运行环境

结果: 😊 "到处都能跑！"
```

### 优势对比

| 特性 | 传统部署 | Docker 部署 |
|------|---------|------------|
| 环境一致性 | ❌ 难以保证 | ✅ 完全一致 |
| 依赖管理 | ❌ 容易冲突 | ✅ 完全隔离 |
| 启动速度 | ⏰ 需要配置环境 | ⚡ 秒级启动 |
| 资源占用 | 💻 直接占用主机 | 📦 轻量级隔离 |
| 可移植性 | ❌ 依赖主机环境 | ✅ 到处可运行 |
| 版本回滚 | ❌ 困难 | ✅ 简单 |

---

## 💡 最佳实践

### 1. Dockerfile 编写

**✅ 推荐做法**:
```dockerfile
# 使用官方基础镜像
FROM python:3.11-slim

# 设置工作目录
WORKDIR /app

# 先复制依赖文件（利用缓存）
COPY requirements.txt .
RUN pip install -r requirements.txt

# 再复制代码（代码变化频繁）
COPY . .

# 使用非 root 用户
RUN useradd -m appuser
USER appuser

# 声明端口
EXPOSE 8000

# 使用 exec 形式的 CMD
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0"]
```

**❌ 避免做法**:
```dockerfile
# 不要使用 latest 标签
FROM python:latest  # ❌

# 不要在一层做太多事
RUN apt-get update && apt-get install -y \
    package1 package2 package3 ...  # ❌

# 不要复制不必要的文件
COPY . .  # 应该使用 .dockerignore
```

### 2. 镜像优化

**减小镜像大小**:
```dockerfile
# 使用多阶段构建
FROM python:3.11 as builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

FROM python:3.11-slim
WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY . .
ENV PATH=/root/.local/bin:$PATH
CMD ["uvicorn", "app.main:app"]
```

**使用 .dockerignore**:
```
# .dockerignore
__pycache__/
*.pyc
.git/
.env
node_modules/
*.log
```

### 3. 容器管理

**命名规范**:
```bash
# 使用有意义的名称
docker run --name backend-api my-app  # ✅
docker run --name abc123 my-app       # ❌

# 使用标签管理版本
docker build -t my-app:v1.0.0 .       # ✅
docker build -t my-app .              # ❌
```

**资源限制**:
```bash
# 限制内存和 CPU
docker run -d \
  --memory="512m" \
  --cpus="1.0" \
  my-app
```

### 4. 数据管理

**使用卷而不是绑定挂载**:
```yaml
# ✅ 推荐：使用命名卷
volumes:
  - app_data:/app/data

# ⚠️ 谨慎：绑定挂载（开发环境可用）
volumes:
  - ./data:/app/data
```

### 5. 安全实践

```dockerfile
# 使用非 root 用户
RUN useradd -m -u 1000 appuser
USER appuser

# 不要在镜像中存储敏感信息
# ❌ ENV SECRET_KEY=abc123
# ✅ 使用环境变量或密钥管理工具

# 定期更新基础镜像
FROM python:3.11-slim  # 定期检查更新
```

---

## 🧹 清理和维护

### 定期清理

```bash
# 清理停止的容器
docker container prune

# 清理未使用的镜像
docker image prune

# 清理未使用的卷
docker volume prune

# 清理未使用的网络
docker network prune

# 一键清理所有
docker system prune -a

# 查看占用空间
docker system df
```

### 监控和调试

```bash
# 查看容器资源使用
docker stats

# 查看容器进程
docker top 容器名称

# 查看容器变化
docker diff 容器名称

# 导出容器文件系统
docker export 容器名称 > container.tar

# 保存镜像
docker save my-app > my-app.tar

# 加载镜像
docker load < my-app.tar
```

---

## 📚 进阶主题

### 1. 多阶段构建

减小最终镜像大小：
```dockerfile
# 构建阶段
FROM node:18 as builder
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

# 运行阶段
FROM node:18-alpine
WORKDIR /app
COPY --from=builder /app/dist ./dist
COPY --from=builder /app/node_modules ./node_modules
CMD ["node", "dist/main.js"]
```

### 2. 健康检查

```dockerfile
HEALTHCHECK --interval=30s --timeout=10s --retries=3 \
  CMD curl -f http://localhost:8000/health || exit 1
```

### 3. 构建参数

```dockerfile
ARG NODE_VERSION=18
FROM node:${NODE_VERSION}

ARG BUILD_DATE
LABEL build_date=${BUILD_DATE}
```

```bash
docker build --build-arg NODE_VERSION=20 -t my-app .
```

### 4. Docker Compose 高级用法

```yaml
services:
  backend:
    build:
      context: .
      args:
        - BUILD_DATE=${BUILD_DATE}
    depends_on:
      redis:
        condition: service_healthy
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
```

---

## 🎓 学习资源

### 官方文档
- [Docker 官方文档](https://docs.docker.com/)
- [Docker Hub](https://hub.docker.com/)
- [Dockerfile 参考](https://docs.docker.com/engine/reference/builder/)

### 项目相关文档
- [agent-backend/docker/README.md](../agent-backend/docker/README.md) - 项目 Docker 配置详解
- [backend-setup.md](./backend-setup.md) - 后端技术架构
- [scripts/startup/README.md](../scripts/startup/README.md) - 启动脚本说明

---

## 🎯 总结

### 核心概念记忆

| 概念 | 房屋比喻 | 一句话总结 |
|------|----------|-----------|
| **Dockerfile** | 建筑图纸 | 定义如何构建镜像的文本文件 |
| **镜像** | 预制模块 | 只读的应用模板，可创建多个容器 |
| **容器** | 实际房屋 | 镜像的运行实例，应用实际运行的地方 |
| **Docker Compose** | 小区规划 | 管理多容器应用的工具 |
| **卷** | 保险箱室 | 持久化数据存储 |
| **网络** | 道路系统 | 容器间通信通道 |
| **端口映射** | 门牌号 | 外部访问容器的入口 |

### 关键理解

1. **镜像 → 容器**: 一个镜像可以创建多个容器
2. **容器 ← 镜像**: 一个容器只能从一个镜像创建
3. **Dockerfile → 镜像**: 图纸用来生产模块
4. **镜像 → 容器**: 模块用来组装房屋

### 下一步

- 🚀 实践：尝试编写自己的 Dockerfile
- 📖 深入：学习 Docker Compose 编排
- 🔧 优化：掌握镜像优化技巧
- 🛡️ 安全：了解 Docker 安全最佳实践

Docker 让应用部署变得简单、一致、可靠。掌握这些核心概念，你就能充分利用容器化技术的优势！🎉
