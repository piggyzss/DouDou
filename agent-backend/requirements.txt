# 核心框架
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.10.5
pydantic-settings==2.7.0
python-dotenv==1.0.0

# 数据库
asyncpg==0.30.0

# HTTP 客户端
requests==2.31.0
httpx==0.25.2

# 网页解析
beautifulsoup4==4.12.2

# RSS 和新闻抓取
feedparser==6.0.11
aiohttp==3.9.1
python-dateutil==2.8.2

# LLM 相关（可选）
google-generativeai>=0.8.3

# 日志
loguru==0.7.2

# 注意：移除了以下可能导致部署问题的依赖
# - aioredis (已弃用)
# - redis (Vercel 环境中不需要)
# - uvicorn (Vercel 自动处理)
# - python-jose (不需要认证)
# - python-multipart (不需要文件上传)
# - pytest 相关 (只在开发环境需要)